const { FEATURE } = require('@forrestjs/hooks')

// define a name for the feature
const FEATURE_NAME = `${FEATURE} feature-todo`

const USER_FEATURE_NAME = `${FEATURE} feature-user`

module.exports = {
    FEATURE_NAME,
    USER_FEATURE_NAME,
}
